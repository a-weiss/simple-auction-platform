<h2>New auction</h2>

<%= render "form.html", changeset: @changeset,
                        action: user_auction_path(@conn, :create, @user) %>

<%= link "Back", to: user_auction_path(@conn, :index, @user) %>
